import { Outlet } from 'react-router-dom';

/**
 * Layout wrapper for the Email & Notifications module
 */
const NotificationsLayout: React.FC = () => {
  return <Outlet />;
};

export default NotificationsLayout;
